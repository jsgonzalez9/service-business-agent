services:
  - type: web
    name: docuseal-wholesale-ai
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: 3000
    disk:
      name: docuseal-data
      mountPath: /data
      sizeGB: 10
